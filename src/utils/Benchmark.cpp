#include "utils/Benchmark.hpp"

float Utils::Benchmark::calculateAverageFps(std::vector<int> &fps_list)
{
    return 0.0f;
}

float Utils::Benchmark::calculateAverageMs(std::vector<float> &ms_list)
{
    return 0.0f;
}